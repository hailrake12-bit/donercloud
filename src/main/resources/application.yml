spring:
  data:
    rest:
     # base-path: /api
      default-page-size: 5
  application:
    name: donercloud
  datasource:
    generate-unique-name: false
    name: donercloud
  jpa:
    defer-datasource-initialization: true
    show-sql: true
    hibernate:
      ddl-auto: create
  jdbc:
    repositories:
      enabled: false

server:
  port: 8333
##  ssl:
  ##  key-store: file:mykeys.jks
    ##key-store-password: letmein
    ##key-password: letmein

logging:
  file:
    path: ./logs/
  level:
    root: INFO
    org:
      springframework:
        security: warn
        jdbc.datasource.init.ScriptUtils: WARN
        data.rest.webmvc: DEBUG

taco:
  orders:
   page-size: 5
